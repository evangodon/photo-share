type Photo {
  id: ID!
  title: String
  url: String!
  album: Album!
}

type Album {
  title: String!
  coverPhoto: String
  photoOrder: [String]!
  photos: [Photo!] @relation
}

type Query {
  allAlbums: [Album!]
  allPhotos: [Photo!]
}
